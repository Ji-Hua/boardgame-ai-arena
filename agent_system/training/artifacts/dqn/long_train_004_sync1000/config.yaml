# Backfilled config for long_train_004_sync1000 (inferred from train.log)
# Canonical benchmark run. target_sync_interval=1000.

episodes: 5000
max_steps_per_episode: 3000  # defaulted

buffer_capacity: 100000
warmup_size: 5000
batch_size: 128
lr: 1.0e-4
gamma: 0.95

epsilon_start: 1.0
epsilon_end: 0.05
epsilon_decay_steps: 250000

target_sync_interval: 1000  # confirmed by run name; ~1333 syncs over ~1333487 opt_steps

checkpoint_dir: agent_system/training/artifacts/dqn/long_train_004_sync1000/checkpoints
checkpoint_interval: 500
agent_id: long_train_004_sync1000  # defaulted

eval_games: 200  # inferred from periodic eval entries
eval_interval: 500

obs_version: v1
model_arch: mlp  # defaulted
hidden_layers: [256, 256]  # defaulted
algorithm: dqn  # defaulted

opponent: random_legal  # defaulted
reward_mode: terminal  # defaulted

seed: 42  # defaulted
log_interval: 100
device: auto
