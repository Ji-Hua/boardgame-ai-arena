# Backfilled config for dqn_mix_dummy_random_d1_20k_001 (inferred from train.log)
# 20k mixed-opponent run.

episodes: 20000
max_steps_per_episode: 3000  # defaulted

buffer_capacity: 100000
warmup_size: 5000
batch_size: 128
lr: 1.0e-4
gamma: 0.95

epsilon_start: 1.0
epsilon_end: 0.05
epsilon_decay_steps: 1000000

target_sync_interval: 200  # defaulted (not specified in log)

checkpoint_dir: agent_system/training/artifacts/dqn/dqn_mix_dummy_random_d1_20k_001/checkpoints
checkpoint_interval: 1000  # inferred from checkpoint filenames
agent_id: dqn_mix_dummy_random_d1_20k_001  # defaulted

eval_games: 200  # inferred from periodic eval entries
eval_interval: 1000  # inferred from eval_episodes=[1000,2000,...]

obs_version: v1
model_arch: mlp  # defaulted
hidden_layers: [256, 256]  # defaulted
algorithm: dqn  # defaulted

opponent: mixed
opponent_mix_dummy: 0.50
opponent_mix_random: 0.45
opponent_mix_minimax_d1: 0.05
opponent_mix_minimax_d2: 0.0  # defaulted

reward_mode: terminal  # defaulted

seed: 42  # defaulted
log_interval: 100
device: auto
