# Backfilled config for long_train_001 (inferred from train.log)
# This is a historical run — parameters inferred from log header and checkpoint files.

episodes: 2000
max_steps_per_episode: 3000  # defaulted

buffer_capacity: 50000
warmup_size: 2000
batch_size: 128
lr: 1.0e-3
gamma: 0.99

epsilon_start: 1.0
epsilon_end: 0.05
epsilon_decay_steps: 100000

target_sync_interval: 500  # inferred: ~1107 syncs over ~553620 opt_steps

checkpoint_dir: agent_system/training/artifacts/dqn/long_train_001/checkpoints
checkpoint_interval: 250  # inferred from checkpoint filenames
agent_id: long_train_001  # defaulted

eval_games: 200  # inferred from periodic eval entries
eval_interval: 250  # inferred from eval_episodes=[250,500,...]

obs_version: v1
model_arch: mlp  # defaulted
hidden_layers: [256, 256]  # defaulted
algorithm: dqn  # defaulted

opponent: random_legal  # defaulted
reward_mode: terminal  # defaulted

seed: 42  # defaulted
log_interval: 50  # inferred from first_metric_ep=50
device: auto
